public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom
shamefully-hoist=true

# drizzle-orm lists `prisma` as a peer it never uses unless you opt into
# @prisma/client integration; auto-install-peers otherwise materialises node_modules/prisma
# (see AUDIT 3.16).
auto-install-peers=false
