node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.dyad/
# pnpm is the declared package manager (package.json#packageManager); npm locks are stale by construction.
package-lock.json

# Local tool caches and one-off screenshots. None of these belong in the repo:
# they are what a previous agent session left behind (.mcp-cache snapshots,
# downloaded llms.txt dumps, playwright MCP state, graph caches, screenshots
# taken while comparing a redesign), and they are 850 files / ~5.5 MB of diff
# that has nothing to do with the application. Coverage and Playwright output
# are generated by `pnpm test:e2e` and were committed on the legacy history.
.mcp-cache/
.playwright-mcp/
.graph-it/
.agents/
agent/skills/
downloads/
supabase/.temp/
coverage/
playwright-report/
test-results/
/auth-*.png
/grid-*.png
